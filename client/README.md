# MTG-Engine client

A Vite + React 19 + TypeScript networked client — see the root `CLAUDE.md`'s "Client architecture"
section for the file map, and its "Commands" section for `npm run dev -w client` / `lab -w client`
/ `build -w client`.
