import { defineCard } from "../define.js";

/** 1/1 colorless Servo artifact creature — Fabricate's token. */
export default defineCard({
  name: "Servo Token",
  colors: [],
  types: ["artifact", "creature"],
  subtypes: ["Servo"],
  power: 1,
  toughness: 1,
});
